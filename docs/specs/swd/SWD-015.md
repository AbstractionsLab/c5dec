---
active: true
derived: false
level: 5.1
links:
- ARC-003: OWpB9vA9eXH9yT3LYKrZaavnsBkg5ObwiR7r1TgQAHY=
- ARC-004: z5iaPfw2-DgGXr3cTFImuHhT_Ublc9l16bzUTYBKIPI=
normative: true
ref: ''
reviewed: IAFkQCtggsYXXPQlX5rgSJr11o7oMHyQD1k_hDfNu5U=
---

# CRA module class diagram

## Overview

The `c5dec.core.cra` module implements the Cyber Resilience Act (EU Regulation
2024/2847) compliance workflow. It provides an object model for CRA requirements
organized by category and section, a loader for the local CRA database, and a
checklist builder that exports an Excel compliance worksheet.

The module also provides helper functions for linking an SBOM to CRA requirements,
enabling automated pre-verification of SBOM-traceable obligations.

## Diagram

```plantuml
@startuml cra_class_diagram
set namespaceSeparator none
hide empty members
skinparam classBackgroundColor LightBlue
skinparam classBorderColor DarkBlue
skinparam ArrowColor DarkBlue
skinparam dpi 150

title "c5dec.core.cra — Class Diagram"

class CRARequirement {
  +id : str
  +name : str
  +text : str
  +applies_to : List[str]
  --
  +applies_to_category(category: str) : bool
}

class CRASection {
  +id : str
  +name : str
  +requirements : List[CRARequirement]
  --
  +get_requirements_for_category(category: str) : List[CRARequirement]
}

class CRACategory {
  +id : str
  +name : str
  +sections : List[CRASection]
  --
  +get_all_requirements() : List[CRARequirement]
  +get_requirements_for_category(category: str) : List[CRARequirement]
}

class CRADatabase {
  +categories : List[CRACategory]
  +db_path : Path
  --
  +load() : None
  +get_all_requirements() : List[CRARequirement]
  +get_requirement_by_id(req_id: str) : Optional[CRARequirement]
  +get_requirements_by_category(category: str) : List[CRARequirement]
}

class CRAChecklistBuilder {
  +db : CRADatabase
  +product_category : str
  --
  +build_checklist(sbom_path: Optional[Path]) : List[Dict]
  +export_to_excel(checklist: List[Dict], output_path: Path) : Path
}

note top of CRADatabase
  Reads YAML/JSON files from
  c5dec/assets/database/CRA/
end note

note right of CRAChecklistBuilder
  Invoked by CLI command
  c5dec cra-checklist
end note

CRADatabase "1" *-- "0..*" CRACategory : contains
CRACategory "1" *-- "1..*" CRASection : contains
CRASection "1" *-- "1..*" CRARequirement : contains
CRAChecklistBuilder --> CRADatabase : uses
CRAChecklistBuilder ..> CRARequirement : iterates

package "c5dec.core.transformer" as transformer <<CORE>> {
}
CRADatabase ..> transformer : uses for data transformation

@enduml
```

## Key design decisions

- `CRADatabase.load()` reads structured YAML/JSON files from
  `c5dec/assets/database/CRA/` and deserializes them into the class hierarchy.
- `CRAChecklistBuilder` delegates SBOM linkage verification to the
  module-level helper functions `verify_sbom_exists`, `link_sbom_to_cra_requirement`,
  and `auto_verify_sbom_requirement`.
- The module relies on `c5dec.core.transformer` for any format conversion during import.
- All path constants are resolved via `c5dec.settings` (`CRA_DB_PATH`, `CRA_LOG_FILE`).