---
active: true
derived: false
level: 3.5
links:
- MRS-058: NFzqVKiHz5GK2ZpA33gS-5rWTdzJPES-eUgpt1Zw13Y=
normative: true
ref: ''
reviewed: RiLDDxI23teZK1sVDSYJWO0-0WF1jTgfx5iMrYwsDwU=
---

# Frontend layer – component diagram

## Description

C5-DEC CAD exposes three user-facing front-ends that share a single command dispatch layer (`commands.py`). The CLI is the primary interface; the TUI and GUI are alternative shells that delegate their business logic to the same command functions. All three front-ends write to a common command log file.

`settings.EXECUTION_MODE` is set at startup to `"CLI"`, `"TUI"`, or `"GUI"` so that core modules can adapt their output formatting accordingly.

## Diagram

```plantuml
@startuml
!theme plain
skinparam componentStyle rectangle
skinparam backgroundColor #FAFAFA
skinparam packageBorderColor #888888

actor User

package "CLI – command-line interface" #DDEEFF {
    component "main.py\nArgparse argument parser;\nregisters all subcommands;\nsets EXECUTION_MODE = CLI" as climain
    component "commands.py\nOne run_<cmd>() function\nper CLI subcommand;\nshared by TUI and GUI" as commands
    component "utils.py\nFormatting helpers,\ncolour output,\ntable rendering" as utils
}

package "TUI – terminal user interface" #DDEEDD {
    component "main.py\nEntry point;\nsets EXECUTION_MODE = TUI" as tuimain
    component "application.py\nasciimatics Screen &\nScene manager" as tuiapp
    package "foundation" {
        component "Base TUI widgets\n(menus, forms, dialogs)" as found
    }
    package "miniapps" {
        component "Feature miniapps\n(one per domain module)" as mini
    }
}

package "GUI – graphical web interface" #FFEECC {
    component "app.py\nFlask application;\nsets EXECUTION_MODE = GUI;\nregisters routes" as guiapp
    component "templates/\nJinja2 HTML templates" as guitpls
    component "static/\nCSS, JS, images" as guistatic
}

package "Shared infrastructure" #F0F0F0 {
    component "c5dec/settings.py\nEXECUTION_MODE,\npath constants,\nlogging config" as settings
    component "c5dec/common.py\nlogger(), HelpFormatter,\nC5decError, feature_flag()" as common
    database "Command log\n(c5dec_cmd.log)" as cmdlog
}

User --> climain    : terminal invocation
User --> tuimain    : --tui flag / direct
User --> guiapp     : browser

climain --> commands    : dispatches
climain --> utils       : formats output
climain --> settings    : reads config

tuimain --> tuiapp      : launches
tuiapp  --> found  : composes screens
tuiapp  --> mini   : renders feature UIs
miniapps --> commands   : delegates domain logic

guiapp  --> guitpls     : renders responses
guiapp  --> guistatic   : serves assets
guiapp  --> commands    : delegates domain logic

commands --> settings   : reads paths & mode
commands --> common     : logs, error handling
commands --> cmdlog     : appends log entries

@enduml
```