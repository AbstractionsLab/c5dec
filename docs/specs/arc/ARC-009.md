---
active: true
derived: false
level: 3.4
links:
- MRS-058: NFzqVKiHz5GK2ZpA33gS-5rWTdzJPES-eUgpt1Zw13Y=
normative: true
ref: ''
reviewed: MIxFhnhFM5LKzqbgnhJXm8539UzjVsQEKQ3tOpA3uCk=
---

# CPSSA subsystem – component diagram

## Description

The Cyber-Physical System Security Assessment (CPSSA) module (`c5dec/core/cpssa/cpssa.py`) is a single-module integration layer that bridges Doorstop architecture artifacts with three external OSS tools: OWASP pytm (threat modelling), Threagile (threat model YAML schema), and pyfair (FAIR-based quantitative risk analysis). The module reads Doorstop ARC/HARC/LARC items from a project's specs tree and produces threat models, DFDs, and risk analysis reports. A YAML mapping file (`threagile-mappings.yml`) translates Doorstop field values to Threagile schema terms; a JSON schema file (`threagile-schema.json`) validates Threagile export output. The module exposes five public functions that are invoked from the CLI via `run_cpssa()` in `cli/commands.py`.

## Diagram

```plantuml
@startuml
!theme plain
skinparam componentStyle rectangle
skinparam backgroundColor #FAFAFA
skinparam packageBorderColor #AAAAAA
skinparam packageBackgroundColor #F8F8F8

package "CPSSA module (c5dec/core/cpssa/cpssa.py)" {

    package "Public API" #EEF0FF {
        component "create_threat_model()\nReads Doorstop ARC/HARC/LARC items;\nproduces Threagile YAML,\npytm Python script, or pytm JSON" as ctm
        component "generate_cpssa_report()\nConsumes Threagile YAML;\ngenerates structured\nCPSSA Markdown report" as gcr
        component "generate_dfd()\nDerives PlantUML DFD\nfrom Doorstop ARC items;\noptionally renders via pytm" as gdfd
        component "generate_fair_input_template()\nExtracts abuse cases from\nthreat model; emits a\ncalibration YAML template" as gfit
        component "run_quantitative_risk_analysis()\nRuns FAIR Monte Carlo sim\n(via pyfair); produces\nALE summary report" as qra
    }

    package "Doorstop reader" #EEFFEE {
        component "_read_doorstop_items()\nParses YAML+MD spec\nitems from a document\ndirectory" as rdi
        component "_find_arc_dir()\n_find_specs_root()\nAuto-discover ARC/HARC/LARC\ndocument path in project" as fad
    }

    package "Model builder" #FFFAEE {
        component "_collect_arc_items()\nFilters raw Doorstop items\nby type (process, datastore,\nexternal, flow, boundary…)" as cai
        component "_build_pytm_model_dict()\nAssembles internal model\ndict with elements, flows,\nboundaries and threat actors" as bpm
        component "_render_pytm_python()\n_render_pytm_json()\nSeriалise model dict to\npytm Python script or JSON" as rpr
    }

    package "Threagile mapping layer" #FFEEFF {
        component "_load_threagile_mappings()\nLazy-loads mapping tables\nfrom threagile-mappings.yml" as ltm
        component "_map_threagile() / field mappers\n(_map_protocol, _map_authentication,\n_map_authorization, _map_technology,\n_map_encryption, _map_machine…)" as mth
    }

    package "FAIR integration" #EEF8FF {
        component "_extract_fair_nodes()\nExtracts PERT parameters\nper abuse-case from YAML" as efn
        component "_supply_fair_inputs()\nBuilds pyfair MetaSimulation\nfrom extracted parameters" as sfi
    }
}

package "Static configuration files" #F5F5F5 {
    component "threagile-mappings.yml\nSchema translation tables\nfor Doorstop → Threagile" as mappings
    component "threagile-schema.json\nThreagile output schema" as schema
}

package "External OSS libraries" #FFFFF0 {
    component "pytm\n(OWASP)\nThreat modelling:\nelements, flows,\nboundaries, threats" as pytm
    component "pyfair\nFAIR quantitative risk\n(Monte Carlo / ALE)" as pyfair_lib
}

database "Doorstop ARC/HARC/LARC\nspec items (YAML + MD)" as doorstop_items

ctm --> fad        : locates spec dir
ctm --> rdi        : reads items
ctm --> cai        : filters items
ctm --> bpm        : builds model dict
ctm --> rpr        : renders pytm output
ctm --> mth        : maps field values
gcr --> rdi        : reads threat model YAML
gdfd --> fad       : locates spec dir
gdfd --> rdi       : reads ARC items
gdfd --> pytm      : optional DFD render
gfit --> rdi       : reads threat model YAML
gfit --> efn       : extracts FAIR nodes
qra --> efn        : extracts FAIR nodes
qra --> sfi        : builds simulation
sfi --> pyfair_lib : runs Monte Carlo

bpm --> mth        : translates field values
ltm --> mappings   : reads file
mth --> ltm        : calls on first use

rdi --> doorstop_items : reads

@enduml
```