---
active: true
derived: false
level: 3.7
links:
- MRS-058: NFzqVKiHz5GK2ZpA33gS-5rWTdzJPES-eUgpt1Zw13Y=
normative: true
ref: ''
reviewed: wcm2BPFkUfjRHhWmjL-65_CQFDqJk0xUjWw2u6Y3eaA=
---

# Deployment diagram

## Description

C5-DEC CAD ships three Docker images that mirror the runtime environments from development to production. The development image and the devcontainer are the primary daily-use targets. A PostQuantum Cryptography (PQC) side-car image provides an `oqs-openssl3` environment for post-quantum operations accessed over a shared Docker network.

## Diagram

```plantuml
@startuml
!theme plain
skinparam nodeBackgroundColor #F5F5F5
skinparam nodeBorderColor #888888
skinparam artifactBackgroundColor #FAFAFA
skinparam componentStyle rectangle

node "Developer workstation\n(VS Code + devcontainer)" {
    node "c5dec-dev container\n(dev.Dockerfile)\nPython 3.11, Poetry, Quarto,\nTeX Live, Doorstop, Syft,\nGnuPG, all dev tools" as devcontainer {
        component "c5dec package\n(poetry install -e .)" as c5decdev
        component "CLI  /  TUI  /  GUI" as frontenddev
    }
    folder "Workspace volume\n(bind-mounted project root)" as vol
    devcontainer -- vol : read/write\n(bind mount)
}

node "CI / CD / production host" {
    node "c5dec container\n(Dockerfile)\nPython 3.11, Poetry,\nminimal runtime" as prodcontainer {
        component "c5dec package" as c5decprod
        component "CLI  /  TUI  /  GUI" as frontendprod
    }
}

node "PQC sidecar\n(shared Docker network)" {
    node "oqs-ossl3 container\n(openquantumsafe/oqs-ossl3)\nOpenSSL 3 + liboqs" as pqccontainer {
        component "Post-quantum\ncrypto operations" as pqc
    }
}

node "DocEngine container\n(docEngine.Dockerfile)\nQuarto + TeX Live only;\nused for rendering\nreports & presentations" as docenginecontainer {
    component "quarto render" as qrender
}

c5decdev --> pqccontainer : PQC requests\n(Docker network)
c5decprod --> pqccontainer : PQC requests\n(Docker network)
c5decdev --> docenginecontainer : quarto render\n(subprocess / volume)
c5decprod --> docenginecontainer : quarto render\n(subprocess / volume)

@enduml
```